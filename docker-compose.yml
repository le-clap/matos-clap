services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      - ENV=production
    volumes:
      - backend-data:/app/data

volumes:
  backend-data:
